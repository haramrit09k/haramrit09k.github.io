{
  "name": "my-portfolio-app",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://haramrit09k.github.io/",
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^16.13.1"
  },
  "scripts": {
    "start": "vite",
    "metrics:generate": "node scripts/generate-project-metrics.mjs",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist -r https://x-access-token:$GH_TOKEN@github.com/haramrit09k/haramrit09k.github.io.git",
    "local-deploy": "gh-pages -d dist",
    "build": "vite build",
    "test": "vitest run"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "gh-pages": "^6.3.0",
    "jsdom": "^27.0.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
